import React from 'react';
import PropTypes from 'prop-types';

const ContentView = (props) => {
    return <div className="rs-content-view">ContentView</div>;
};

ContentView.propTypes = {};

export default ContentView;
